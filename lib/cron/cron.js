'use strict'
const startAll = () => new Promise((resolve, reject) => {
  resolve(true)
})

const stopAll = () => new Promise((resolve, reject) => {
  resolve(true)
})

const startItem = () => new Promise((resolve, reject) => {
  resolve(true)
})

const stopItem = () => new Promise((resolve, reject) => {
  resolve(true)
})

const executeItem = () => new Promise((resolve, reject) => {
  resolve(true)
})

module.exports = {
  startAll,
  stopAll,
  startItem,
  stopItem,
  executeItem
};
